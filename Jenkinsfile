pipeline {

    agent any

    stages {

        stage ("build"){
            steps {
                echo 'This pipeline script is triggered via GitHub webhook'
                sh 'echo integrating Jenkins pipeline via bla bla bla'
            }
        }
    }
}